- bg-linear-to is supported using tailwind, whille bg-gradient-to is deprecated
- using zod v4, we need to specify special subsets of strings and numbers like z.email() or z.url() instead of z.string().url() -  although both are valid, the latter is deprecated.
